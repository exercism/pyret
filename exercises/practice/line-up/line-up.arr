use context essentials2020 # Don't delete this line when using Pyret on Exercism 

provide:
  format-message
end

fun format-message(person, position):
  raise("please implement the format-message function")
end

use context essentials2020 # Don't delete this line when using Pyret on Exercism 

provide: encode, decode end

fun encode(phrase):
  raise("please implement the encode function")
end

fun decode(phrase):
  raise("please implement the decode function")
end
